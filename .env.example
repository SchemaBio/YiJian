# YiJian frontend environment
# Copy to .env.local for local development, or pass to the Docker container.

# Runtime browser-visible Squid API base URL.
# Use /api when a reverse proxy routes /api to Squid.
YIJIAN_API_URL=/api

# Octopus core API prefix under the selected backend.
# - Direct Octopus: leave empty (frontend calls /api/v1/tasks, /api/v1/samples, ...)
# - Squid gateway: set /v1/octopus (frontend calls /api/v1/octopus/tasks, ...)
# If left empty, the client retries /v1/octopus once after a core API returns 404.
YIJIAN_CORE_API_PREFIX=

# UI/backend compatibility mode: auto | octopus | squid.
# Squid mode keeps SaaS organization fields required on registration.
YIJIAN_BACKEND_FLAVOR=auto

# Local Next.js dev fallback. Docker runtime uses YIJIAN_API_URL.
NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_CORE_API_PREFIX=
NEXT_PUBLIC_BACKEND_FLAVOR=auto

# Must match Squid CLIENT_PASSWORD_HASH_ENABLED.
NEXT_PUBLIC_PASSWORD_HASH_ENABLED=false

# Set to "false" to skip the privacy consent checkbox on the login page.
# Default: true (privacy consent required).
NEXT_PUBLIC_PRIVACY_CONSENT_REQUIRED=true

# Set to "true" to enable mock login for local development (no backend required).
# Ignored automatically when NODE_ENV=production.
# Any email/password combination will work.
NEXT_PUBLIC_DEV_MOCK_AUTH=false

# Next.js runtime
PORT=3000
HOSTNAME=0.0.0.0
NEXT_TELEMETRY_DISABLED=1
